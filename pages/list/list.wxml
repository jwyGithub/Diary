<view class="list">
	<van-collapse accordion value="{{ activeName }}" bind:change="onChange">
		<van-collapse-item title="{{item.title}}" name="{{index}}" value="{{ item.time }}" wx:for="{{records}}"
			wx:key="index">
			{{item.msg}}
		</van-collapse-item>
	</van-collapse>
</view>